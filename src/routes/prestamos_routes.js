const {  } = require('../controllers/prestamos_control');
import {auth} from "../../middelware/auth";

const router = require('express').Router();

// router.get("/multas_por_grupo", auth, get_multas_por_grupo);
export default router;